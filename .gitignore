node_modules
dist
coverage
*.log
.DS_Store
.claude/
.wrangler/
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*

# raw (pre-processed) sprite generations — the processed PNGs in the parent dir are committed
game/assets/sprites/raw/

# brainstorming visual-companion mockups
.superpowers/
